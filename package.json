{
  "name": "horimetro-cambui",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "prepare:standalone": "node prepare_standalone.js",
    "build": "npm run prepare:standalone",
    "build:web": "npm run prepare:standalone",
    "build:android": "npm run build && npx cap sync && cd android && gradlew assembleDebug",
    "sync:android": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/core": "^5.7.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
